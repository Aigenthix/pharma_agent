version: '3.8'

services:
  pharmaassist:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./frontend:/app/frontend
